#include "karl.h"

int karl_daemon(int argc, char** argv)
{
/*	int sockfd;
	struct addrinfo hints, *servinfo, *p;
	socklen_t sin_size;
	struct sigaction sa;
	int yes = 1;
	char s[INET6_ADDRSTRLEN];
	int rv;

	memset(&hints, 0, sizeof(hints);
	hints.ai_family = AF_UNSPEC;
*/
	return 0;
}
